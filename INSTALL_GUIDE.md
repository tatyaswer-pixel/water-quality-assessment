# 📖 คู่มือการติดตั้งและใช้งานระบบประเมินคุณภาพน้ำทิ้ง

## 🎯 ขั้นตอนที่ 1: เตรียมความพร้อม

### ✅ สิ่งที่ต้องมี:
1. **Python 3.8 หรือสูงกว่า** (คุณมี Python 3.13.0 แล้ว ✓)
2. **Text Editor** - VS Code (คุณมีแล้ว ✓)
3. **Command Prompt หรือ PowerShell**
4. **โฟลเดอร์โปรเจค** - water-quality-assessment

---

## 🚀 ขั้นตอนที่ 2: การติดตั้ง

### 2.1 แตกไฟล์โปรเจค
1. แตกไฟล์ `water-quality-assessment.zip` ที่ดาวน์โหลดมา
2. วางโฟลเดอร์ไว้ที่ตำแหน่งที่สะดวก เช่น:
   ```
   C:\Users\YourName\Documents\water-quality-assessment
   ```

### 2.2 เปิด Command Prompt
1. กด `Windows + R`
2. พิมพ์ `cmd` แล้วกด Enter
3. เข้าไปในโฟลเดอร์โปรเจค:
   ```bash
   cd C:\Users\YourName\Documents\water-quality-assessment
   ```

### 2.3 สร้าง Virtual Environment (แนะนำให้ทำ)
```bash
python -m venv venv
```

**รอสักครู่** จนกว่าจะเสร็จ (ประมาณ 30 วินาที)

### 2.4 เปิดใช้งาน Virtual Environment
**สำหรับ Windows:**
```bash
venv\Scripts\activate
```

**ถ้าสำเร็จ** จะเห็น `(venv)` ด้านหน้าบรรทัดคำสั่ง:
```
(venv) C:\Users\YourName\Documents\water-quality-assessment>
```

### 2.5 ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

**รอสักครู่** จนกว่าจะติดตั้งเสร็จ (ประมาณ 2-3 นาที)

### 2.6 เริ่มต้นฐานข้อมูล
```bash
python init_db.py
```

**ผลลัพธ์ที่ควรเห็น:**
```
============================================================
🚀 เริ่มต้นฐานข้อมูล Water Quality Assessment System
============================================================

📦 กำลังสร้างตารางในฐานข้อมูล...
✅ สร้าง Admin account สำเร็จ (username: admin, password: password: ใช้ค่า ADMIN_PASSWORD ที่ตั้งไว้ใน environment)

📝 กำลังสร้างข้อมูลตัวอย่าง...
✅ สร้างข้อมูลตัวอย่าง 3 รายการสำเร็จ

============================================================
✅ เริ่มต้นฐานข้อมูลเรียบร้อย!
============================================================

📌 บัญชี Admin:
   ชื่อผู้ใช้: admin
   รหัสผ่าน: รหัสผ่าน: ใช้ค่า ADMIN_PASSWORD ที่ตั้งไว้ใน environment

🚀 รันโปรแกรมด้วยคำสั่ง: python app.py
============================================================
```

---

## 🎮 ขั้นตอนที่ 3: รันโปรแกรม

### 3.1 เริ่มเซิร์ฟเวอร์
```bash
python app.py
```

**ผลลัพธ์ที่ควรเห็น:**
```
 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment.
 * Running on all addresses (0.0.0.0)
 * Running on http://127.0.0.1:5000
 * Running on http://192.168.x.x:5000
Press CTRL+C to quit
```

### 3.2 เปิดเว็บเบราว์เซอร์
1. เปิด Google Chrome, Firefox หรือ Edge
2. พิมพ์ URL:
   ```
   http://localhost:5000
   ```
   หรือ
   ```
   http://127.0.0.1:5000
   ```

### 3.3 เข้าสู่ระบบ
- **ชื่อผู้ใช้:** `admin`
- **รหัสผ่าน:** `ADMIN_PASSWORD`

---

## 📚 ขั้นตอนที่ 4: การใช้งาน

### 4.1 ทดสอบกรอกข้อมูลด้วยมือ
1. คลิกเมนู **"กรอกข้อมูล"**
2. กรอกข้อมูลตัวอย่าง:
   - **วันที่:** (จะตั้งค่าอัตโนมัติ)
   - **สถานที่:** โรงพยาบาล A
   - **pH:** 7.5
   - **BOD:** 15
   - **COD:** 100
   - **TSS:** 25
   - **TDS:** 800
   - **O&G:** 18
   - **TKN:** 30
   - **Sulfide:** 0
   - **TCB:** 4000
   - **FCB:** 800
   - **Cl₂:** 0.8
3. คลิก **"บันทึกและประเมินผล"**
4. ดูผลการประเมิน

### 4.2 ทดสอบอัปโหลด Excel
1. คลิกเมนู **"อัปโหลด Excel"**
2. เลือกไฟล์ **`sample_data.xlsx`** ที่มีในโฟลเดอร์โปรเจค
3. คลิก **"อัปโหลดและประเมินผล"**
4. ระบบจะประมวลผลและบันทึกข้อมูลทั้งหมด

### 4.3 ดู Dashboard
1. คลิกเมนู **"Dashboard"**
2. จะเห็น:
   - สถิติโดยรวม (ทั้งหมด, ผ่าน, ใกล้เกณฑ์, ไม่ผ่าน)
   - กราฟแนวโน้ม 30 วันล่าสุด
   - ตารางข้อมูล 10 รายการล่าสุด

### 4.4 ดูประวัติ
1. คลิกเมนู **"ประวัติ"**
2. จะเห็นรายการข้อมูลทั้งหมด
3. คลิก **"ดูผล"** เพื่อดูรายละเอียด

---

## ❗ การแก้ปัญหาที่พบบ่อย

### ปัญหา 1: ไม่สามารถเปิดใช้งาน Virtual Environment
**วิธีแก้:**
```bash
# ใช้คำสั่งนี้แทน
venv\Scripts\activate.bat
```

หรือถ้ายังไม่ได้ ให้ข้ามขั้นตอน Virtual Environment ไปเลย และติดตั้ง library โดยตรง:
```bash
pip install -r requirements.txt
```

### ปัญหา 2: Error "No module named 'flask'"
**สาเหตุ:** ยังไม่ได้ติดตั้ง dependencies

**วิธีแก้:**
```bash
pip install -r requirements.txt
```

### ปัญหา 3: Port 5000 ถูกใช้งานแล้ว
**สาเหตุ:** โปรแกรมอื่นใช้ port 5000 อยู่

**วิธีแก้:**
1. เปิดไฟล์ `app.py`
2. ไปที่บรรทัดสุดท้าย
3. เปลี่ยนจาก:
   ```python
   app.run(debug=True, host='0.0.0.0', port=5000)
   ```
   เป็น:
   ```python
   app.run(debug=True, host='0.0.0.0', port=5001)
   ```
4. บันทึกไฟล์
5. เปิดเว็บที่: `http://localhost:5001`

### ปัญหา 4: ไม่สามารถอัปโหลด Excel ได้
**สาเหตุ:** รูปแบบไฟล์ Excel ไม่ถูกต้อง

**วิธีแก้:**
- ตรวจสอบว่ามีคอลัมน์ครบตามที่กำหนด
- แถวแรกต้องเป็นชื่อคอลัมน์
- ข้อมูลเริ่มจากแถวที่ 2

### ปัญหา 5: หน้าเว็บไม่แสดงผล
**วิธีแก้:**
1. ปิดเซิร์ฟเวอร์ (กด Ctrl+C ใน Command Prompt)
2. รันใหม่:
   ```bash
   python app.py
   ```
3. Refresh หน้าเว็บ (กด F5)

---

## 🛑 การหยุดโปรแกรม

1. ไปที่ Command Prompt ที่รันโปรแกรมอยู่
2. กด `Ctrl + C`
3. พิมพ์ `Y` แล้วกด Enter (ถ้าถาม)

---

## 🔄 การรันโปรแกรมครั้งต่อไป

**ไม่ต้องติดตั้งใหม่** เพียงแค่:

1. เปิด Command Prompt
2. เข้าไปในโฟลเดอร์โปรเจค:
   ```bash
   cd C:\Users\YourName\Documents\water-quality-assessment
   ```
3. เปิดใช้งาน Virtual Environment (ถ้าใช้):
   ```bash
   venv\Scripts\activate
   ```
4. รันโปรแกรม:
   ```bash
   python app.py
   ```
5. เปิดเว็บที่: `http://localhost:5000`

---

## 📝 สร้างไฟล์ Excel สำหรับอัปโหลด

ไฟล์ Excel ต้องมีโครงสร้างดังนี้:

| sample_date | sample_location | sample_type | pH | BOD | COD | TSS | TDS | O&G | TKN | Sulfide | TCB | FCB | Cl2 | NH4 | DO |
|------------|----------------|-------------|----|----|-----|-----|-----|-----|-----|---------|-----|-----|-----|-----|-----|
| 2025-01-15 10:00:00 | โรงพยาบาล A | ทางออก | 7.5 | 15 | 100 | 25 | 800 | 18 | 30 | 0 | 4000 | 800 | 0.8 | 5 | 6.5 |

**หมายเหตุ:**
- **sample_date** ต้องเป็นรูปแบบ: YYYY-MM-DD HH:MM:SS
- **คอลัมน์ NH4 และ DO** สามารถเว้นว่างได้ (ไม่บังคับ)
- **ห้ามเว้นว่าง** ในคอลัมน์อื่นๆ

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Error Message ใน Command Prompt
2. อ่าน Error แล้วแก้ไขตามคำแนะนำ
3. ดูในไฟล์ README.md เพิ่มเติม

---

## ✅ Checklist การติดตั้งสำเร็จ

- [ ] ติดตั้ง Python เสร็จ
- [ ] แตกไฟล์โปรเจคเสร็จ
- [ ] สร้าง Virtual Environment เสร็จ
- [ ] ติดตั้ง Dependencies เสร็จ (pip install)
- [ ] รัน init_db.py เสร็จ (เห็นข้อความสำเร็จ)
- [ ] รัน app.py ได้ (เห็น Running on http://127.0.0.1:5000)
- [ ] เปิดเว็บได้ (เห็นหน้า Login)
- [ ] Login ได้ (ใช้ admin / รหัสผ่านที่กำหนดผ่าน ADMIN_PASSWORD)
- [ ] กรอกข้อมูลได้
- [ ] อัปโหลด Excel ได้
- [ ] ดู Dashboard ได้

**ถ้าทำได้ครบทั้งหมด แสดงว่าติดตั้งสำเร็จ! 🎉**

---

**อัปเดตล่าสุด:** 10 พฤศจิกายน 2025  
**เวอร์ชัน:** 1.0.0
